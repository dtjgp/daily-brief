{
  "name": "daily-brief",
  "version": "0.1.0",
  "private": true,
  "description": "Automated daily multi-source brief",
  "scripts": {
    "sync:x": "node scripts/sync-x-report.mjs",
    "build": "node scripts/generate-daily-brief.mjs",
    "build:full": "npm run sync:x && npm run build"
  }
}
